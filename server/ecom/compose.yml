services:
  app:
    image: ${IMAGE}
    container_name: ${COMPOSE_PROJECT_NAME:-imonitor-ecom-erp}
    restart: unless-stopped
    ports:
      - "8081:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
